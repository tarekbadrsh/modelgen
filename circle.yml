version: 2

jobs:
  build:
    docker:
      - image: circleci/golang
        environment:
          GO111MODULE: "ON"
    working_directory: /go/src/github.com/tarekbadrshalaan/modelgen
    steps:
      - checkout